# kernel/

This directory contains the canonical MMS kernel contract in operational form.

It is the implementation-adjacent companion to:
- `docs/10_kernel-contract.md`

It should eventually contain:
- stable contract text fragments
- invariants
- ID rules
- (optionally) reference validators

Nothing in here defines truth or decision authority.

